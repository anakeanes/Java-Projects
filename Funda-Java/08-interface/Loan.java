// 'interface' will not have the real implementation, it will be just a definition
// Any class want to use interface, will have exactly the same structure as the following
public interface Loan {
    public double calculateInterest();  
}
